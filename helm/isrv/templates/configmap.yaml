apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "isrv.fullname" . }}
  labels:
    {{- include "isrv.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "isrv.configFile" . | nindent 4 }}
